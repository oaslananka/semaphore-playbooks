- name: Update & upgrade packages
  hosts: all
  become: true

  tasks:
    - name: Update package list
      apt:
        update_cache: yes

    - name: Upgrade all packages
      apt:
        upgrade: dist
